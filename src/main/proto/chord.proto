syntax = "proto3";

option java_multiple_files = true;
option java_package = "se.umu.cs.ads.chord";

import "google/protobuf/empty.proto";

package chord;

service ChordService {
	rpc healthCheck(google.protobuf.Empty) returns (HealthCheckResponse); // Check status of node
	rpc findSuccessor(Identifier) returns (Node);
	rpc getPredecessor(google.protobuf.Empty) returns (GetPredecessorResponse);
	rpc notify(Node) returns (google.protobuf.Empty);
}

message HealthCheckResponse {
	bool status = 1;
}

// An identifier in the hash space
message Identifier {
	bytes value = 1;
}

message Node {
	Identifier identifier = 1;
	string address = 2;
}

message GetPredecessorResponse {
	optional Node node = 1;
}
